{
  "version": 2,
  "buildCommand": "pnpm run build && pnpm run build-storybook",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/storybook/(.*)",
      "dest": "/storybook-static/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}